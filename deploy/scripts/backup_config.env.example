# 备份与灾备配置示例（复制为 backup_config.env 后按实际修改，勿提交敏感值）
# 供 backup_runner.sh、restore_full.sh 等读取

# ---------- 备份类型 ----------
# full=全量 incremental=增量（基于上次全量后的变更）
BACKUP_TYPE=full

# ---------- 备份目标：逗号分隔 local,s3,nas ----------
BACKUP_TARGETS=local

# ---------- 本地目录 ----------
BACKUP_DIR=/backup/paas
# 全量保留天数，增量保留天数
BACKUP_RETENTION_FULL_DAYS=7
BACKUP_RETENTION_INCR_DAYS=3

# ---------- 对象存储 S3/OSS ----------
# BACKUP_S3_BUCKET=my-bucket
# BACKUP_S3_PREFIX=paas/backup
# BACKUP_S3_ENDPOINT=   # OSS 可选，如 https://oss-cn-hangzhou.aliyuncs.com
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=cn-north-1

# ---------- NAS 存储（挂载点或 rsync 目标） ----------
# BACKUP_NAS_PATH=/mnt/nas/paas_backup
# 或 rsync: BACKUP_NAS_RSYNC=user@nas-server:/volume/paas_backup

# ---------- 告警（备份失败时） ----------
# BACKUP_ALERT_WEBHOOK=   # 企业微信/钉钉/Slack webhook
# BACKUP_ALERT_EMAIL=     # 可选邮件

# ---------- 细胞列表（与 backup_cells.sh 一致） ----------
BACKUP_CELLS=crm erp oa srm wms mes tms plm ems his lis lims hrm
