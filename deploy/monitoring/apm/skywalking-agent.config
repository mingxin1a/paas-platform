# SkyWalking Python Agent 配置示例（网关/Cell 使用）
# 使用方式：pip install apache-skywalking；启动前设置环境变量
# export SW_AGENT_NAME=gateway
# export SW_AGENT_COLLECTOR_BACKEND_SERVICES=oap:11800
# python -m skywalking bootstrap deploy/run_gateway.py

# 或通过 env 注入（Docker/K8s）
# SW_AGENT_NAME=gateway
# SW_AGENT_COLLECTOR_BACKEND_SERVICES=oap:11800
# SW_AGENT_LOGGING_LEVEL=INFO

# 服务名建议：gateway | governance | crm-cell | erp-cell | ... | lims-cell
