# SPA 前端静态资源服务（Vite 构建产物）
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
    location /api {
        # 生产环境由网关统一转发，此处仅托管静态资源
        return 404;
    }
}
