# OA 商用交付手册

**版本**：1.0 | **细胞**：协同办公（OA） | **对象**：交付/运维

## 功能概述

- 组织架构、用户权限；审批流程（审批单创建、提交、审批）；公告；日历；文件管理。
- 多租户隔离；审批支持幂等；数据级权限（X-User-Id）。

## 部署与运维

- 镜像：`cells/oa`；端口默认 8005；网关配置 `CELL_OA_URL`。
- 健康：`/health`；运维与备份见总手册。

**详细**：见 `cells/oa/docs/OA商用交付手册.md`。
