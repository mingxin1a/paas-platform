# HRM 细胞 - 用户手册（骨架）

**版本**：0.1.0

## 概述

人力资源管理细胞，提供员工、部门、请假申请等 API，经 PaaS 网关 `/api/v1/hrm` 暴露。对标 OrangeHRM 通用能力，实现遵循 SuperPaaS 架构（独立 Schema、仅网关交互）。

## API

- /health：健康检查
- /employees：员工列表、创建
- /departments：部门列表、创建
- /leave-requests：请假申请列表、创建

请求需 X-Tenant-Id；POST 需 X-Request-ID（幂等）。
